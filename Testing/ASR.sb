#!/bin/bash --login
########## SBATCH Lines for Resource Request ##########
#SBATCH --time=45:00:00# limit of wall clock time - how long the job will run (same as -t)
#SBATCH --nodes=1-5# number of different nodes - could be an exact number or a range of nodes (same as -N)
#SBATCH --ntasks=1# number of tasks - how many tasks (nodes) that you require (same as -n)
#SBATCH --cpus-per-task=3# number of CPUs (or cores) per task (same as -c)
#SBATCH --mem-per-cpu=25G# memory required per allocated CPU (or core) - amount of memory (in bytes)
#SBATCH --job-name AutoASR_test# you can give your job a name for easier identification (same as -J)
########## Command Lines for Job Running ##########
module purge
ml -* GCC/10.2.0 OpenMPI/4.0.5 Biopython/1.78 MAFFT/7.475 CD-HIT/4.8.1 IQ-TREE/2.1.3 matplotlib/3.3.3
cd /mnt/home/vanant25/Documents/Research/Automated\ ASR/Experiments/Testing #Write the path to where you want this to run on this line here!
python -u AutoASR.py input.fasta 400 0.7 0.5
scontrol show job $SLURM_JOB_ID
js -j $SLURM_JOB_ID
